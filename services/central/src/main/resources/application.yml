central:
  endpoint:
    temp-api: localhost:8081

spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem://localhost/~/test_db;
      username: sa
      password: pwd
      driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true